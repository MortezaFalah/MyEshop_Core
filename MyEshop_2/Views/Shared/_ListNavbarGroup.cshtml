@using MyEshop_2.Data.Repositories
@inject IGroupRepository _groupRepository


<li class="nav-item dropdown">
    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown"
    aria-haspopup="true" aria-expanded="false">
        گروه ها
    </a>
    <div class="dropdown-menu dropdown-menu-left" aria-labelledby="navbarDropdown" aria-labelledby="dropdown09">
        @foreach(var item in _groupRepository.GetAllCategories())
        {
            <a class="dropdown-item" href="/Group/@item.id/@item.name">@item.name</a>
        }
    </div>
</li>

@*
<li class="nav-item dropdown ">
    <a class="nav-link dropdown-toggle" href="http://example.com" id="dropdown09" data-toggle="dropdown"
    aria-haspopup="true" aria-expanded="false">گروه ها</a>
    <div class="dropdown-menu" aria-labelledby="dropdown09">
        @foreach (var item in _groupRepository.GetAllCategories())
        {
            <a class="dropdown-item" href="/Group/@item.id/@item.name">@item.name</a>
        }
    </div>
</li>*@
              